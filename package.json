{
  "name": "matriz-energetica",
  "version": "1.0.0",
  "description": "Dashboard en tiempo real de generación energética CAMMESA — San Juan",
  "private": true,
  "scripts": {
    "backend": "cd backend && node src/server.js",
    "backend:dev": "cd backend && npx nodemon src/server.js",
    "frontend": "cd frontend && npx vite",
    "frontend:build": "cd frontend && npm run build",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  }
}
